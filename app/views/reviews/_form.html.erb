<%= simple_form_for([@play,@play.reviews.build]) do |f| %>
	<div class="raitng-form">
		<%= f.input :rating %>
	</div>
	<%= f.input :comment %>
	<%= f.button :submit  , :class => "btn-custom2"%>

<% end %>
